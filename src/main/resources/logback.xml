<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="rollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/tmp/logs/cwm.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>/tmp/logs/cwm.%d{yyyy-MM-dd}.log</fileNamePattern>
        </rollingPolicy>
        <encoder>
            <pattern>%X{logback.sessionId} %X{logback.ip} %date{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- project default level -->
    <logger name="com.winit.cwm" level="DEBUG"/>
	<logger name="org.springframework" level="info"/>
	<logger name="com.alibaba.druid" level="info"/>
	<logger name="org.springframework.amqp.rabbit" level="error"/>
    <root level="DEBUG">
           <appender-ref ref="console"/> 
        <appender-ref ref="rollingFile"/>
    </root>
</configuration>